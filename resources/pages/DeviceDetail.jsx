import React from 'react';

// Placeholder — Role 3 will replace this with the full DeviceDetail component.
// Named component slot — keep this file so routing works during integration.
export default function DeviceDetail() {
    return (
        <div style={{ padding: '32px', fontFamily: 'Inter, sans-serif', color: '#E2E8F0' }}>
            <p style={{ color: '#94A3B8' }}>Device detail — Role 3 component slot.</p>
        </div>
    );
}